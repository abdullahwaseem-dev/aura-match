# Secrets — never commit these, on any platform, ever.
.env
.env.local
*.env
!.env.example

# Node (server/)
node_modules/
npm-debug.log*

# Tesseract OCR language data — downloaded/cached at runtime, not source.
*.traineddata

# OS cruft
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
